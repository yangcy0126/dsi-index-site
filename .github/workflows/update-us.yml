name: Update US WDSI Data

on:
  push:
    branches:
      - main
    paths:
      - ".github/workflows/update-us.yml"
      - "scripts/**"
      - "requirements.txt"
  schedule:
    - cron: "35 15 * * *"
  workflow_dispatch:

concurrency:
  group: update-wdsi-us-${{ github.ref }}
  cancel-in-progress: false

permissions:
  contents: write

jobs:
  update-us:
    runs-on: ubuntu-latest
    timeout-minutes: 360
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: python -m pip install -r requirements.txt

      - name: Install browser fallback
        run: |
          python -m pip install playwright
          python -m playwright install chromium

      - name: Check API secret
        run: |
          if [ -z "${WDSI_API_KEY}" ] && [ -z "${OPENAI_API_KEY}" ]; then
            echo "WDSI_API_KEY or OPENAI_API_KEY secret is missing."
            exit 1
          fi
        env:
          WDSI_API_KEY: ${{ secrets.WDSI_API_KEY }}
          OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}

      - name: Update records (standard)
        run: |
          python -u scripts/update_wdsi_records.py \
            --countries US \
            --lookback-days 90 \
            --max-pages 40 \
            --history-backfill-rounds 0 \
            --skip-build
        env:
          WDSI_API_KEY: ${{ secrets.WDSI_API_KEY }}
          OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}
          WDSI_API_BASE_URL: ${{ vars.WDSI_API_BASE_URL }}
          OPENAI_BASE_URL: ${{ vars.OPENAI_BASE_URL }}
          WDSI_MODEL: ${{ vars.WDSI_MODEL }}
          WDSI_OPENAI_MODEL: ${{ vars.WDSI_OPENAI_MODEL }}
          WDSI_REASONING_EFFORT: ${{ vars.WDSI_REASONING_EFFORT }}

      - name: Upload country record checkpoint
        uses: actions/upload-artifact@v4
        with:
          name: records-US
          path: records/US.csv
          if-no-files-found: error
          overwrite: true

      - name: Refresh records (modern current reparse)
        continue-on-error: true
        run: |
          python -u scripts/update_wdsi_records.py \
            --countries US \
            --start-date 2025-01-20 \
            --end-date "$(date -u +%F)" \
            --max-pages 120 \
            --history-backfill-rounds 0 \
            --skip-build
        env:
          WDSI_API_KEY: ${{ secrets.WDSI_API_KEY }}
          OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}
          WDSI_API_BASE_URL: ${{ vars.WDSI_API_BASE_URL }}
          OPENAI_BASE_URL: ${{ vars.OPENAI_BASE_URL }}
          WDSI_MODEL: ${{ vars.WDSI_MODEL }}
          WDSI_OPENAI_MODEL: ${{ vars.WDSI_OPENAI_MODEL }}
          WDSI_REASONING_EFFORT: ${{ vars.WDSI_REASONING_EFFORT }}

      - name: Upload post-modern-current checkpoint
        uses: actions/upload-artifact@v4
        with:
          name: records-US
          path: records/US.csv
          if-no-files-found: error
          overwrite: true

      - name: Refresh records (modern archive late-2024 reparse)
        continue-on-error: true
        run: |
          python -u scripts/update_wdsi_records.py \
            --countries US \
            --start-date 2024-08-01 \
            --end-date 2025-01-19 \
            --max-pages 140 \
            --history-backfill-rounds 0 \
            --skip-build
        env:
          WDSI_API_KEY: ${{ secrets.WDSI_API_KEY }}
          OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}
          WDSI_API_BASE_URL: ${{ vars.WDSI_API_BASE_URL }}
          OPENAI_BASE_URL: ${{ vars.OPENAI_BASE_URL }}
          WDSI_MODEL: ${{ vars.WDSI_MODEL }}
          WDSI_OPENAI_MODEL: ${{ vars.WDSI_OPENAI_MODEL }}
          WDSI_REASONING_EFFORT: ${{ vars.WDSI_REASONING_EFFORT }}

      - name: Upload post-modern-archive-late-2024 checkpoint
        uses: actions/upload-artifact@v4
        with:
          name: records-US
          path: records/US.csv
          if-no-files-found: error
          overwrite: true

      - name: Refresh records (modern archive early-2024 reparse)
        continue-on-error: true
        run: |
          python -u scripts/update_wdsi_records.py \
            --countries US \
            --start-date 2024-01-01 \
            --end-date 2024-07-31 \
            --max-pages 140 \
            --history-backfill-rounds 0 \
            --skip-build
        env:
          WDSI_API_KEY: ${{ secrets.WDSI_API_KEY }}
          OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}
          WDSI_API_BASE_URL: ${{ vars.WDSI_API_BASE_URL }}
          OPENAI_BASE_URL: ${{ vars.OPENAI_BASE_URL }}
          WDSI_MODEL: ${{ vars.WDSI_MODEL }}
          WDSI_OPENAI_MODEL: ${{ vars.WDSI_OPENAI_MODEL }}
          WDSI_REASONING_EFFORT: ${{ vars.WDSI_REASONING_EFFORT }}

      - name: Upload post-modern-archive-early-2024 checkpoint
        uses: actions/upload-artifact@v4
        with:
          name: records-US
          path: records/US.csv
          if-no-files-found: error
          overwrite: true

      - name: Refresh records (modern archive late-2023 reparse)
        continue-on-error: true
        run: |
          python -u scripts/update_wdsi_records.py \
            --countries US \
            --start-date 2023-07-01 \
            --end-date 2023-12-31 \
            --max-pages 140 \
            --history-backfill-rounds 0 \
            --skip-build
        env:
          WDSI_API_KEY: ${{ secrets.WDSI_API_KEY }}
          OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}
          WDSI_API_BASE_URL: ${{ vars.WDSI_API_BASE_URL }}
          OPENAI_BASE_URL: ${{ vars.OPENAI_BASE_URL }}
          WDSI_MODEL: ${{ vars.WDSI_MODEL }}
          WDSI_OPENAI_MODEL: ${{ vars.WDSI_OPENAI_MODEL }}
          WDSI_REASONING_EFFORT: ${{ vars.WDSI_REASONING_EFFORT }}

      - name: Upload post-modern-archive-late-2023 checkpoint
        uses: actions/upload-artifact@v4
        with:
          name: records-US
          path: records/US.csv
          if-no-files-found: error
          overwrite: true

      - name: Refresh records (modern archive early-2023 reparse)
        continue-on-error: true
        run: |
          python -u scripts/update_wdsi_records.py \
            --countries US \
            --start-date 2023-01-01 \
            --end-date 2023-06-30 \
            --max-pages 140 \
            --history-backfill-rounds 0 \
            --skip-build
        env:
          WDSI_API_KEY: ${{ secrets.WDSI_API_KEY }}
          OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}
          WDSI_API_BASE_URL: ${{ vars.WDSI_API_BASE_URL }}
          OPENAI_BASE_URL: ${{ vars.OPENAI_BASE_URL }}
          WDSI_MODEL: ${{ vars.WDSI_MODEL }}
          WDSI_OPENAI_MODEL: ${{ vars.WDSI_OPENAI_MODEL }}
          WDSI_REASONING_EFFORT: ${{ vars.WDSI_REASONING_EFFORT }}

      - name: Upload post-modern-archive-early-2023 checkpoint
        uses: actions/upload-artifact@v4
        with:
          name: records-US
          path: records/US.csv
          if-no-files-found: error
          overwrite: true

      - name: Refresh records (modern archive late-2022 reparse)
        continue-on-error: true
        run: |
          python -u scripts/update_wdsi_records.py \
            --countries US \
            --start-date 2022-07-01 \
            --end-date 2022-12-31 \
            --max-pages 160 \
            --history-backfill-rounds 0 \
            --skip-build
        env:
          WDSI_API_KEY: ${{ secrets.WDSI_API_KEY }}
          OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}
          WDSI_API_BASE_URL: ${{ vars.WDSI_API_BASE_URL }}
          OPENAI_BASE_URL: ${{ vars.OPENAI_BASE_URL }}
          WDSI_MODEL: ${{ vars.WDSI_MODEL }}
          WDSI_OPENAI_MODEL: ${{ vars.WDSI_OPENAI_MODEL }}
          WDSI_REASONING_EFFORT: ${{ vars.WDSI_REASONING_EFFORT }}

      - name: Upload post-modern-archive-late-2022 checkpoint
        uses: actions/upload-artifact@v4
        with:
          name: records-US
          path: records/US.csv
          if-no-files-found: error
          overwrite: true

      - name: Refresh records (modern archive early-2022 reparse)
        continue-on-error: true
        run: |
          python -u scripts/update_wdsi_records.py \
            --countries US \
            --start-date 2022-03-01 \
            --end-date 2022-06-30 \
            --max-pages 120 \
            --history-backfill-rounds 0 \
            --skip-build
        env:
          WDSI_API_KEY: ${{ secrets.WDSI_API_KEY }}
          OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}
          WDSI_API_BASE_URL: ${{ vars.WDSI_API_BASE_URL }}
          OPENAI_BASE_URL: ${{ vars.OPENAI_BASE_URL }}
          WDSI_MODEL: ${{ vars.WDSI_MODEL }}
          WDSI_OPENAI_MODEL: ${{ vars.WDSI_OPENAI_MODEL }}
          WDSI_REASONING_EFFORT: ${{ vars.WDSI_REASONING_EFFORT }}

      - name: Upload post-modern-archive-early-2022 checkpoint
        uses: actions/upload-artifact@v4
        with:
          name: records-US
          path: records/US.csv
          if-no-files-found: error
          overwrite: true

      - name: Update records (extra history backfill)
        continue-on-error: true
        run: |
          python -u scripts/update_wdsi_records.py \
            --countries US \
            --lookback-days 1 \
            --max-pages 90 \
            --history-backfill-rounds 1 \
            --history-only \
            --skip-build
        env:
          WDSI_API_KEY: ${{ secrets.WDSI_API_KEY }}
          OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}
          WDSI_API_BASE_URL: ${{ vars.WDSI_API_BASE_URL }}
          OPENAI_BASE_URL: ${{ vars.OPENAI_BASE_URL }}
          WDSI_MODEL: ${{ vars.WDSI_MODEL }}
          WDSI_OPENAI_MODEL: ${{ vars.WDSI_OPENAI_MODEL }}
          WDSI_REASONING_EFFORT: ${{ vars.WDSI_REASONING_EFFORT }}

      - name: Upload post-extra checkpoint
        uses: actions/upload-artifact@v4
        with:
          name: records-US
          path: records/US.csv
          if-no-files-found: error
          overwrite: true

      - name: Update records (second extra history backfill)
        continue-on-error: true
        run: |
          python -u scripts/update_wdsi_records.py \
            --countries US \
            --lookback-days 1 \
            --max-pages 90 \
            --history-backfill-rounds 1 \
            --history-only \
            --skip-build
        env:
          WDSI_API_KEY: ${{ secrets.WDSI_API_KEY }}
          OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}
          WDSI_API_BASE_URL: ${{ vars.WDSI_API_BASE_URL }}
          OPENAI_BASE_URL: ${{ vars.OPENAI_BASE_URL }}
          WDSI_MODEL: ${{ vars.WDSI_MODEL }}
          WDSI_OPENAI_MODEL: ${{ vars.WDSI_OPENAI_MODEL }}
          WDSI_REASONING_EFFORT: ${{ vars.WDSI_REASONING_EFFORT }}

      - name: Upload post-second-extra checkpoint
        uses: actions/upload-artifact@v4
        with:
          name: records-US
          path: records/US.csv
          if-no-files-found: error
          overwrite: true

      - name: Update records (third extra history backfill)
        continue-on-error: true
        run: |
          python -u scripts/update_wdsi_records.py \
            --countries US \
            --lookback-days 1 \
            --max-pages 90 \
            --history-backfill-rounds 1 \
            --history-only \
            --skip-build
        env:
          WDSI_API_KEY: ${{ secrets.WDSI_API_KEY }}
          OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}
          WDSI_API_BASE_URL: ${{ vars.WDSI_API_BASE_URL }}
          OPENAI_BASE_URL: ${{ vars.OPENAI_BASE_URL }}
          WDSI_MODEL: ${{ vars.WDSI_MODEL }}
          WDSI_OPENAI_MODEL: ${{ vars.WDSI_OPENAI_MODEL }}
          WDSI_REASONING_EFFORT: ${{ vars.WDSI_REASONING_EFFORT }}

      - name: Upload post-third-extra checkpoint
        uses: actions/upload-artifact@v4
        with:
          name: records-US
          path: records/US.csv
          if-no-files-found: error
          overwrite: true

      - name: Update records (fourth extra history backfill)
        continue-on-error: true
        run: |
          python -u scripts/update_wdsi_records.py \
            --countries US \
            --lookback-days 1 \
            --max-pages 90 \
            --history-backfill-rounds 1 \
            --history-only \
            --skip-build
        env:
          WDSI_API_KEY: ${{ secrets.WDSI_API_KEY }}
          OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}
          WDSI_API_BASE_URL: ${{ vars.WDSI_API_BASE_URL }}
          OPENAI_BASE_URL: ${{ vars.OPENAI_BASE_URL }}
          WDSI_MODEL: ${{ vars.WDSI_MODEL }}
          WDSI_OPENAI_MODEL: ${{ vars.WDSI_OPENAI_MODEL }}
          WDSI_REASONING_EFFORT: ${{ vars.WDSI_REASONING_EFFORT }}

      - name: Upload post-fourth-extra checkpoint
        uses: actions/upload-artifact@v4
        with:
          name: records-US
          path: records/US.csv
          if-no-files-found: error
          overwrite: true

      - name: Update records (fifth extra history backfill)
        continue-on-error: true
        run: |
          python -u scripts/update_wdsi_records.py \
            --countries US \
            --lookback-days 1 \
            --max-pages 90 \
            --history-backfill-rounds 1 \
            --history-only \
            --skip-build
        env:
          WDSI_API_KEY: ${{ secrets.WDSI_API_KEY }}
          OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}
          WDSI_API_BASE_URL: ${{ vars.WDSI_API_BASE_URL }}
          OPENAI_BASE_URL: ${{ vars.OPENAI_BASE_URL }}
          WDSI_MODEL: ${{ vars.WDSI_MODEL }}
          WDSI_OPENAI_MODEL: ${{ vars.WDSI_OPENAI_MODEL }}
          WDSI_REASONING_EFFORT: ${{ vars.WDSI_REASONING_EFFORT }}

      - name: Upload post-fifth-extra checkpoint
        uses: actions/upload-artifact@v4
        with:
          name: records-US
          path: records/US.csv
          if-no-files-found: error
          overwrite: true

      - name: Update records (sixth extra history backfill)
        continue-on-error: true
        run: |
          python -u scripts/update_wdsi_records.py \
            --countries US \
            --lookback-days 1 \
            --max-pages 90 \
            --history-backfill-rounds 1 \
            --history-only \
            --skip-build
        env:
          WDSI_API_KEY: ${{ secrets.WDSI_API_KEY }}
          OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}
          WDSI_API_BASE_URL: ${{ vars.WDSI_API_BASE_URL }}
          OPENAI_BASE_URL: ${{ vars.OPENAI_BASE_URL }}
          WDSI_MODEL: ${{ vars.WDSI_MODEL }}
          WDSI_OPENAI_MODEL: ${{ vars.WDSI_OPENAI_MODEL }}
          WDSI_REASONING_EFFORT: ${{ vars.WDSI_REASONING_EFFORT }}

      - name: Upload post-sixth-extra checkpoint
        uses: actions/upload-artifact@v4
        with:
          name: records-US
          path: records/US.csv
          if-no-files-found: error
          overwrite: true

      - name: Update records (seventh extra history backfill)
        continue-on-error: true
        run: |
          python -u scripts/update_wdsi_records.py \
            --countries US \
            --lookback-days 1 \
            --max-pages 90 \
            --history-backfill-rounds 1 \
            --history-only \
            --skip-build
        env:
          WDSI_API_KEY: ${{ secrets.WDSI_API_KEY }}
          OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}
          WDSI_API_BASE_URL: ${{ vars.WDSI_API_BASE_URL }}
          OPENAI_BASE_URL: ${{ vars.OPENAI_BASE_URL }}
          WDSI_MODEL: ${{ vars.WDSI_MODEL }}
          WDSI_OPENAI_MODEL: ${{ vars.WDSI_OPENAI_MODEL }}
          WDSI_REASONING_EFFORT: ${{ vars.WDSI_REASONING_EFFORT }}

      - name: Upload post-seventh-extra checkpoint
        uses: actions/upload-artifact@v4
        with:
          name: records-US
          path: records/US.csv
          if-no-files-found: error
          overwrite: true

      - name: Update records (eighth extra history backfill)
        continue-on-error: true
        run: |
          python -u scripts/update_wdsi_records.py \
            --countries US \
            --lookback-days 1 \
            --max-pages 90 \
            --history-backfill-rounds 1 \
            --history-only \
            --skip-build
        env:
          WDSI_API_KEY: ${{ secrets.WDSI_API_KEY }}
          OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}
          WDSI_API_BASE_URL: ${{ vars.WDSI_API_BASE_URL }}
          OPENAI_BASE_URL: ${{ vars.OPENAI_BASE_URL }}
          WDSI_MODEL: ${{ vars.WDSI_MODEL }}
          WDSI_OPENAI_MODEL: ${{ vars.WDSI_OPENAI_MODEL }}
          WDSI_REASONING_EFFORT: ${{ vars.WDSI_REASONING_EFFORT }}

      - name: Upload post-eighth-extra checkpoint
        uses: actions/upload-artifact@v4
        with:
          name: records-US
          path: records/US.csv
          if-no-files-found: error
          overwrite: true

      - name: Update records (ninth extra history backfill)
        continue-on-error: true
        run: |
          python -u scripts/update_wdsi_records.py \
            --countries US \
            --lookback-days 1 \
            --max-pages 90 \
            --history-backfill-rounds 1 \
            --history-only \
            --skip-build
        env:
          WDSI_API_KEY: ${{ secrets.WDSI_API_KEY }}
          OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}
          WDSI_API_BASE_URL: ${{ vars.WDSI_API_BASE_URL }}
          OPENAI_BASE_URL: ${{ vars.OPENAI_BASE_URL }}
          WDSI_MODEL: ${{ vars.WDSI_MODEL }}
          WDSI_OPENAI_MODEL: ${{ vars.WDSI_OPENAI_MODEL }}
          WDSI_REASONING_EFFORT: ${{ vars.WDSI_REASONING_EFFORT }}

      - name: Upload country record
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: records-US
          path: records/US.csv
          if-no-files-found: error
          overwrite: true

  finalize:
    runs-on: ubuntu-latest
    needs: update-us
    if: always()
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: python -m pip install -r requirements.txt

      - name: Download US record
        continue-on-error: true
        uses: actions/download-artifact@v4
        with:
          name: records-US
          path: records

      - name: Rebuild site data
        run: python scripts/build_wdsi_data.py

      - name: Check method lock
        run: python scripts/check_method_lock.py

      - name: Commit and push
        run: |
          git restore --source=HEAD -- data/visitor_stats.json || true
          if git diff --quiet; then
            echo "No US data changes detected."
          else
            git config user.name "github-actions[bot]"
            git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
            git add records/US.csv data
            git commit -m "Update US WDSI data"
            for attempt in 1 2 3; do
              if git pull --rebase origin main && git push; then
                exit 0
              fi
              git rebase --abort || true
              echo "Push attempt ${attempt} failed; retrying..."
              sleep 5
            done
            echo "Failed to push rebuilt US data after 3 attempts."
            exit 1
          fi
